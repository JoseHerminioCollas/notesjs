var expect = require('chai').expect
const Note = require('goatstone/notes/note')

describe('Notes Test', function () {
    it('should....', () => {
        Note()
        expect(1).to.equal(1)
    })
})

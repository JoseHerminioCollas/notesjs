var expect = require('chai').expect
const [n, m] = require('../goatstone/notes/note')

describe('Notes Test', function () {

    it('should...', () => {
        console.log(n(1), m)
        expect(1).to.equal(1)
    })
})
